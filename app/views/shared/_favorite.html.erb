<%= form_for(current_user.favorites.build(micropost_id: micropost.id) , remote: true) do |f| %>
<div><%= f.hidden_field :micropost_id %></div>
<%= f.submit "Favorite", class: "btn btn-small btn-primary" %>
<% end %>