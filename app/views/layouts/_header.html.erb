<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Application", root_path, id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><%= link_to "Home", root_path %></li>
          <% if signed_in? %>
            <li><%= link_to "@mentions", mentions_user_path(current_user) %></li>
          <li>
            <%= form_tag users_path, method: 'get' do %>
              <%= text_field_tag :search, params[:search], class: "search-query inputsearch", placeholder: "search" %>  
            <% end %>
          </li>
           <!--  <li><%= link_to "Users", users_path %></li> -->
            <li id="fat-menu" class="dropdown">
              <!-- <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                Account <b class="caret"></b> -->
              <a href="#" class="dropdown-toggle" data-toggle="dropdown">
              <i class="icon-cog icon-white"></i> <b class="caret"></b>
              </a>
              <ul class="dropdown-menu">
                <li><%= link_to "Profile", current_user %></li>
                <li><%= link_to "Settings", edit_user_path(current_user) %></li>
                <li class="divider"></li>
                <li><%= link_to "Help", help_path%></li>
                <li class="divider"></li>
                <li>
                  <%= link_to "Sign Out", signout_path, method: "delete" %>
                </li>
              </ul>
            </li>
          <% else %>
            <li><%= link_to "Help", help_path %></li>
            <li><%= link_to "Sign In", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>